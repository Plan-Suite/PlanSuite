@page "/settings/access-denied"
@model AccessDeniedModel
@{
    ViewData["Title"] = "Access Denied";
}

<div class="container mb-5 mt-5">
    <h1 class="text-center"><i class="bi bi-lock"></i><br />@ViewData["Title"].ToString().ToUpper()</h1>
    <p class="text-muted text-center">You currently do not have access to this page.<br />Please try again later.</p>
</div>
